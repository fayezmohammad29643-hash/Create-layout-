name: Build Quran Layouts

on:
  workflow_dispatch:
  push:
    paths:
      - 'input/mushaf.docx'
      - 'scripts/**'
      - 'config.json'
      - '.github/workflows/build-layout.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout this repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Brotli fallback support
        run: python -m pip install --disable-pip-version-check Brotli

      - name: Build all surah layouts
        run: |
          python scripts/build_layout.py \
            --docx input/mushaf.docx \
            --config config.json \
            --output-dir output \
            --jobs 6

      - name: Validate generated layouts
        run: python scripts/check_layout.py output

      - name: Commit generated JSON files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add output/
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "build: regenerate Quran surah layouts [skip ci]"
            git push
          fi
